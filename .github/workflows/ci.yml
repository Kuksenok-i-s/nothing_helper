name: CI

on:
  push:
  pull_request:

jobs:
  build-test-linux:
    name: Linux — vet / build / test -race
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Vet
        run: go vet ./...

      - name: Build
        run: go build ./...

      - name: Test (race)
        run: go test -race ./...

  build-test-macos:
    name: macOS — vet / build / test
    runs-on: macos-14
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Vet
        run: go vet ./...

      - name: Build
        run: go build ./...

      - name: Test
        run: go test ./...

      - name: Build companion with menu bar
        run: go build -tags "gio systray" ./cmd/tws_manager

  linux-package-deb:
    name: Linux — Debian package (amd64)
    runs-on: ubuntu-latest
    needs: build-test-linux
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Install build dependencies
        run: sudo make install-deps-debian

      - name: Check companion GUI
        run: |
          go vet -tags gio ./internal/ui/companion
          go test -race -tags gio ./internal/ui/companion

      - name: Build Debian package
        env:
          ARCH: amd64
        run: make package-deb
